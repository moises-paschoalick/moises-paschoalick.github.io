window.initCodeInteraction = function() {
    const codeBlock = document.getElementById('code-block');
    if (!codeBlock) return;

    // Lógica para adicionar tooltips ou destaques baseados em regras
    // Por enquanto, vamos apenas garantir que o container exista
    console.log('Interatividade de código inicializada');
    
    // Futuro: Adicionar parsing de [tags] no JSON para criar spans clicáveis
};
