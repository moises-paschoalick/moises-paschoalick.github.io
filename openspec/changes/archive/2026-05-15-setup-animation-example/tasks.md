## 1. Preparação

- [x] 1.1 Validar a presença do arquivo `jquery-3.4.1.min.js` na raiz.
- [x] 1.2 Validar a presença do arquivo `animacao.js` na raiz.

## 2. Implementação

- [x] 2.1 Criar o arquivo `exemplo_animacao.html` na raiz do projeto.
- [x] 2.2 Estruturar o HTML básico com as tags `<head>` e `<body>`.
- [x] 2.3 Adicionar os estilos CSS para garantir que o canvas ocupe 100% da tela e o fundo seja preto.
- [x] 2.4 Incluir as tags `<script>` na ordem correta: jQuery local, Three.js R68 (CDN), e `animacao.js` local.

## 3. Validação

- [x] 3.1 Abrir `exemplo_animacao.html` no navegador e verificar se a animação é renderizada.
- [x] 3.2 Testar a interatividade: movimento do mouse, clique e teclas de seta.
