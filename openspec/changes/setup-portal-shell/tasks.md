## 1. Estrutura de Arquivos e CSS

- [x] 1.1 Criar diretórios `styles/`, `js/` e `content/`.
- [x] 1.2 Limpar o `index.html` e adicionar a estrutura base (sidebar + main content).
- [x] 1.3 Implementar o CSS base em `styles/main.css` (Layout Grid, reset, tipografia).
- [x] 1.4 Adicionar estilos para a Sidebar (itens ativos, hover, transições).

## 2. Navegação e Dados

- [x] 2.1 Criar arquivo `content/topics.json` com a lista dos 27 títulos e IDs.
- [x] 2.2 Implementar a lógica de carregamento do menu lateral em `js/navigation.js`.
- [x] 2.3 Implementar a troca de conteúdo (Fetch do JSON do tópico + renderização no DOM).
- [x] 2.4 Adicionar botões "Anterior" e "Próximo" com lógica de navegação circular.

## 3. Interatividade de Código e Conteúdo Base

- [x] 3.1 Criar o primeiro arquivo de conteúdo: `content/01-alto-acoplamento.json`.
- [x] 3.2 Implementar a lógica de tooltips em `js/code-interact.js`.
- [x] 3.3 Adicionar estilização para os cards de "Dor" e "Cura".
- [x] 3.4 Validar o funcionamento de um link para o GitHub em um bloco de código.

## 4. Polimento e Validação

- [x] 4.1 Testar a responsividade básica (mobile drawer).
- [x] 4.2 Garantir que o estado da URL (hash) reflita o tópico atual para permitir bookmarks.
