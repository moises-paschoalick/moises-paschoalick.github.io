## 1. Dados e Lógica

- [x] 1.1 Atualizar `content/topics.json` adicionando `"completed": true` para os IDs `01-alto-acoplamento`, `05-dependency-injection` e `07-factory-method`.
- [x] 1.2 Modificar a função `renderSidebar` em `js/navigation.js` para adicionar a classe `completed` se o tópico tiver a flag.

## 2. Interface e Estilo

- [x] 2.1 Adicionar estilos CSS em `styles/main.css` para a classe `.completed`.
- [x] 2.2 Usar `::after` para exibir o ícone (checkmark) de forma alinhada.

## 3. Validação

- [x] 3.1 Verificar se os tópicos 1, 5 e 7 exibem o ícone na sidebar.
- [x] 3.2 Verificar se os outros tópicos permanecem sem o ícone.
- [x] 3.3 Garantir que o ícone continue visível quando o item está no estado `active`.
